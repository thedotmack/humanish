{
  "name": "broker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit && node ../../scripts/check-action-allowlist.mjs",
    "test": "vitest run",
    "lint": "echo skip"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251008.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.9",
    "wrangler": "^4.0.0"
  }
}
